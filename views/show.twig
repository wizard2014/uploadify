{% extends 'templates/app.twig' %}

{% block main %}
    <div class="thumbnail">
        <img src="{{ app.uploadcare.getFile(image.url).getUrl }}" alt="" class="thumbnail__image">
    </div>
{% endblock %}